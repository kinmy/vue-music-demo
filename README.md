# 第十章 搜索页面开发
```
学习内容：
vuex
QQ搜索接口获取（使用jsonp获取xml数据）
上拉加载
splice（）会改变原数组
apply的用法
debounce节流函数（减少流量消耗，优化用户体验）
浏览器本地缓存localStorage
findIndex（fn）所有子项都会执行fn
选择弹窗的数据统计函数开发（确认+取消的统计）
```
```
失误：
无
```
   
# Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

# build for production and view the bundle analyzer report
npm run build --report
```
