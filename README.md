# 第七章 播放器内置组件开发
```
vuex
QQ歌词获取（修改请求头referer）
css前缀兼容补全
js钩子插件 create-keyframe-animation
audio
补“0”算法
svg圆形进度条
shuffle洗牌算法
base-64插件
lyric-parse插件
vue功能mixin
```
   
# Build Setup

``` bash
# install dependencies
npm install

# serve with hot reload at localhost:8080
npm run dev

# build for production with minification
npm run build

# build for production and view the bundle analyzer report
npm run build --report
```
